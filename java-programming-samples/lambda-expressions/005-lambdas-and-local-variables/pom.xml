<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.joolzminer</groupId>
		<artifactId>lambda-expressions</artifactId>
		<version>1.0-SNAPSHOT</version>
	</parent>

    <artifactId>005-lambdas-and-local-variables</artifactId>

    <packaging>jar</packaging>

    <name>|-- 005-lambdas-and-local-variables</name>
	<description>	
        Keywords:
            + Lambdas, lambda expressions
            + Local variables.

        This project includes a simple example that illustrates the use of local
        variables in a lambda expression.
            
        LambdaRunner:
            Illustrates how a lambda can reference a static variable, an instance
            variable or a local variable that is final or effectively final.
	</description>
	
    <properties>
        <!-- JDK compiler options -->
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
    </properties>	
</project>

