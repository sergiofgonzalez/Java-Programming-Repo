<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.joolzminer</groupId>
		<artifactId>functional</artifactId>
		<version>1.0-SNAPSHOT</version>
	</parent>

    <artifactId>005-observer</artifactId>

    <packaging>jar</packaging>

    <name>|-- 005-observer</name>
	<description>	
        Keywords:
            + Functional Programming, Java 8
            + Observer Pattern

        This project includes a very simple example that illustrates how to implement
        the Observer pattern using lambdas.
            
        FeedRunner:
            In the example, an Feed system in charge of sending tweets to news agencies is
            developed. The Observer implementation can be done using lambdas, thus removing
            the need for an implementation class.
	</description>
	
    <properties>
        <!-- JDK compiler options -->
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
    </properties>	
</project>

